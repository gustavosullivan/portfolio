# Currículo (arquivo morto — não faz parte do site)

Pasta local para o HTML/PDF do currículo.
Não é rota do Next.js, não entra em `public/` e não aparece no site em produção.
Serve só pra versionar e puxar em qualquer máquina (`git pull`).

Abrir: `curriculo.html` no navegador.
