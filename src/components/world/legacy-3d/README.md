# Legacy 3D world (saved)

Ideia original: planeta de grama 3D girando + guerreiro low-poly RPG andando + ciclo dia/noite.

Arquivos aqui foram arquivados de propósito — a versão atual do site usa a cena 2D anime em `WorldStage.tsx` / `AnimeParade.tsx`.

Para reativar a versão 3D no futuro, importe `WorldCanvas` desta pasta no `WorldStage`.
